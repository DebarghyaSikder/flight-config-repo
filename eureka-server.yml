server:
  port: 8761

spring:
  application:
    name: eureka-server

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      # Eureka refers to itself via container name
      defaultZone: http://eureka-server:8761/eureka
  instance:
    hostname: eureka-server
